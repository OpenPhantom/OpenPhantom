/* hud_ratio_scaling.h: one global size knob for the HUD, and square glyphs everywhere.
 *
 * Produces: hud_ratio_scaling.dll  (replaces the earlier text_aspect_fix.dll)
 */
#ifndef HUD_RATIO_SCALING_H
#define HUD_RATIO_SCALING_H

void hud_ratio_scaling_install(void);

#endif /* HUD_RATIO_SCALING_H */
